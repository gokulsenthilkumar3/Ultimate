Based on the repository's `task.md` upgrade checklist, here's the complete list of pending changes that still need to be done — everything not yet marked complete. [claude](https://claude.ai/new)

## Phase 3: Health & Fitness (Incomplete)

- `Nutrition.jsx` — build 3-part macro ring chart, add dynamic BMR tracking
- `Physique.jsx` — implement body fat % calculation from measurements
- `Medical.jsx` — build health timeline visualizer
- `HabitsMatrix.jsx` — improve the 365-day GitHub-style heatmap
- `GoalsDashboard.jsx` — add milestone sub-tasks and confetti animation on goal completion
- `Training.jsx` — finish progressive overload tracking (currently marked in-progress)

## Phase 4: Finance & Specialty (All Incomplete)

- `Finance.tsx` — add budget thresholds, monospaced typography, interactive donut chart
- `SIPCalculator.jsx` — add inflation-adjusted returns and interactive sliders
- `Projects.jsx` — implement Gantt chart logic and Kanban toggle view
- `AiDashboard.jsx` — add typing effect, semantic search, prompt caching
- `Tasks.jsx` — build Eisenhower Matrix 4-quadrant view
- `Notes.jsx` — add Markdown parsing, tag filtering, auto-save
- `Skills.jsx` — build RPG-style skill trees, XP curves, particle level-up effect
- `Timesheet.jsx` — add stopwatch, billable hour calculation, block timeline
- `Calendar.jsx` — add recurring events, iCal export, premium grid view
- `Analytics.jsx` — build cross-domain correlation (e.g., Sleep vs Productivity)
- `Portfolio.jsx` — add sparklines, ROI calculations, mocked ticker
- `Shopping.jsx` — add price-drop tracking and visual priority tags
- `Logs.jsx` — add markdown rendering and sentiment filtering
- `Databases.jsx` — build Notion-style sortable table with CSV import/export
- `AppLauncher.jsx` — add frequent-app sorting and dock magnification hover effect
- `Overview.jsx` — optimize aggregation logic, add hero "day at a glance" section
- `About.jsx` — add changelog timeline with staggered version history
- `StrengthMetrics.jsx` — add 1RM calculators and muscle fatigue heatmap
- `Current.jsx` — add time-of-day gradients and location/weather syncing

## Already Completed (for reference, no action needed)

- Phase 1: premium.css enhancements, global animation keyframes — done
- Phase 2: `Documents.jsx`, `SleepDashboard.jsx`, `SocialMedia.jsx`, `Entertainment.jsx` — done
- Phase 3 (partial): `HydrationTracker.jsx`, `MindWellness.jsx` — done

## Additional Context from feature.md

Separately, the repo has a much larger unimplemented spec in `feature.md` for a "Photorealistic 360° Parametric Human Engine" — a 5-phase 3D body visualization system (Sprite3DViewer, Blender-style body customizer, anatomical peel/organ viewer, face-clone/bio-feedback shaders, wardrobe + ghost comparison system). This is a much bigger build-out (Three.js/React-Three-Fiber, GLB models, custom shaders) and isn't part of the simple task checklist — it reads as a standalone engineering brief you'd tackle separately if you want the full 3D body-twin feature, not just incremental UI fixes. [claude](https://claude.ai/new)

You now have the full unchecked list from `task.md` (23 component tasks across Phases 3–4) plus the separate large `feature.md` 3D engine spec — you can reply telling me which specific item(s) you want built first and I'll draft the implementation plan or code for it.