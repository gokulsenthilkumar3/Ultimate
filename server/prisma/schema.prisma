generator client {
  provider   = "prisma-client-js"
  engineType = "binary"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model User {
  id            Int      @id @default(autoincrement())
  email         String   @unique
  password_hash String
  created_at    DateTime @default(now())
  source        String   @default("manual")
  confidence    Float    @default(1.0)
  updated_at    DateTime @default(now()) @updatedAt

  @@map("users")
}

model Task {
  id          Int      @id @default(autoincrement())
  title       String
  description String?
  priority    String?
  tag         String?
  dueDate     String?
  done        Boolean  @default(false)
  recurring   Boolean  @default(false)
  frequency   String?
  lastDone    String?
  completedAt String?
  created_at  DateTime @default(now())
  created_by  String   @default("user")
  modified_at DateTime @default(now())
  modified_by String   @default("user")
  source      String   @default("manual")
  confidence  Float    @default(1.0)
  updated_at  DateTime @default(now()) @updatedAt

  @@map("tasks")
}

model ShoppingItem {
  id            Int      @id @default(autoincrement())
  name          String
  category      String?
  priority      String?
  estimatedCost Float?
  quantity      Int      @default(1)
  purchased     Boolean  @default(false)
  created_at    DateTime @default(now())
  created_by    String   @default("user")
  modified_at   DateTime @default(now())
  modified_by   String   @default("user")
  source        String   @default("manual")
  confidence    Float    @default(1.0)
  updated_at    DateTime @default(now()) @updatedAt

  @@map("shopping")
}

model Timesheet {
  id          Int       @id @default(autoincrement())
  task        String?
  duration    Int?
  date        DateTime?
  created_at  DateTime  @default(now())
  created_by  String    @default("user")
  modified_at DateTime  @default(now())
  modified_by String    @default("user")
  source      String    @default("manual")
  confidence  Float     @default(1.0)
  updated_at  DateTime  @default(now()) @updatedAt

  @@map("timesheet")
}

model Finance {
  id         String    @id @default(uuid())
  type       String
  category   String?
  amount     Float
  note       String?
  method     String?
  date       DateTime?
  created_at DateTime  @default(now())
  source     String    @default("manual")
  confidence Float     @default(1.0)
  updated_at DateTime  @default(now()) @updatedAt

  @@map("finance")
}

model Budget {
  id           String   @id @default(uuid())
  category     String
  limit_amount Float
  period       String   @default("monthly")
  created_at   DateTime @default(now())
  source       String   @default("manual")
  confidence   Float    @default(1.0)
  updated_at   DateTime @default(now()) @updatedAt

  @@map("budgets")
}

model MetricLog {
  id         Int      @id @default(autoincrement())
  userId     Int?
  metric     String
  value      Float
  unit       String?
  source     String?
  date       String
  created_at DateTime @default(now())
  confidence Float    @default(1.0)
  updated_at DateTime @default(now()) @updatedAt

  @@map("metric_logs")
}

model AuditLog {
  id          Int      @id @default(autoincrement())
  action      String?
  table_name  String?
  item_id     String?
  details     String?
  timestamp   DateTime @default(now())
  actor_name  String   @default("System")
  actor_email String   @default("admin@growthtrack.ultimate")
  actor_ip    String?
  source      String   @default("manual")
  confidence  Float    @default(1.0)
  updated_at  DateTime @default(now()) @updatedAt

  @@map("audit_log")
}

model UserProfile {
  id          Int      @id @default(autoincrement())
  data        String
  created_at  DateTime @default(now())
  created_by  String   @default("system")
  modified_at DateTime @default(now())
  modified_by String   @default("system")
  source      String   @default("manual")
  confidence  Float    @default(1.0)
  updated_at  DateTime @default(now()) @updatedAt

  @@map("user_profile")
}

model TrainingPlan {
  id         Int      @id @default(autoincrement())
  data       String
  source     String   @default("manual")
  confidence Float    @default(1.0)
  updated_at DateTime @default(now()) @updatedAt

  @@map("training_plan")
}

model NutritionStrategy {
  id         Int      @id @default(autoincrement())
  data       String
  source     String   @default("manual")
  confidence Float    @default(1.0)
  updated_at DateTime @default(now()) @updatedAt

  @@map("nutrition_strategy")
}

model LifestyleTips {
  id         Int      @id @default(autoincrement())
  data       String
  source     String   @default("manual")
  confidence Float    @default(1.0)
  updated_at DateTime @default(now()) @updatedAt

  @@map("lifestyle_tips")
}

model MedicalData {
  id         Int      @id @default(autoincrement())
  data       String
  source     String   @default("manual")
  confidence Float    @default(1.0)
  updated_at DateTime @default(now()) @updatedAt

  @@map("medical_data")
}

model PhysiqueTargets {
  id         Int      @id @default(autoincrement())
  data       String
  source     String   @default("manual")
  confidence Float    @default(1.0)
  updated_at DateTime @default(now()) @updatedAt

  @@map("physique_targets")
}

model AssessmentQA {
  id         Int      @id @default(autoincrement())
  data       String
  source     String   @default("manual")
  confidence Float    @default(1.0)
  updated_at DateTime @default(now()) @updatedAt

  @@map("assessment_qa")
}

model Skills {
  id         Int      @id @default(autoincrement())
  data       String
  source     String   @default("manual")
  confidence Float    @default(1.0)
  updated_at DateTime @default(now()) @updatedAt

  @@map("skills")
}

model CalendarEvents {
  id         Int      @id @default(autoincrement())
  data       String
  source     String   @default("manual")
  confidence Float    @default(1.0)
  updated_at DateTime @default(now()) @updatedAt

  @@map("calendar_events")
}

model Note {
  id          Int      @id @default(autoincrement())
  title       String   @default("Untitled")
  content     String?  @default("")
  color       String?  @default("#e5a50a")
  pinned      Boolean? @default(false)
  created_at  DateTime @default(now())
  modified_at DateTime @default(now())
  source      String   @default("manual")
  confidence  Float    @default(1.0)
  updated_at  DateTime @default(now()) @updatedAt

  @@map("notes")
}

model Goal {
  id            Int               @id @default(autoincrement())
  title         String
  category      String?           @default("General")
  target_value  Float?
  current_value Float?            @default(0)
  unit          String?
  deadline      DateTime?
  done          Boolean?          @default(false)
  created_at    DateTime          @default(now())
  modified_at   DateTime          @default(now())
  progressLogs  GoalProgressLog[]
  source        String            @default("manual")
  confidence    Float             @default(1.0)
  updated_at    DateTime          @default(now()) @updatedAt

  @@map("goals")
}

model SleepLog {
  id         Int      @id @default(autoincrement())
  date       DateTime @unique
  bed_time   String?
  wake_time  String?
  duration   Float?
  quality    Int?
  notes      String?
  created_at DateTime @default(now())
  source     String   @default("manual")
  confidence Float    @default(1.0)
  updated_at DateTime @default(now()) @updatedAt

  @@map("sleep_logs")
}

model Document {
  id         Int      @id @default(autoincrement())
  name       String
  size       String?
  date       String?
  type       String?  @default("Private")
  url        String?
  created_at DateTime @default(now())
  source     String   @default("manual")
  confidence Float    @default(1.0)
  updated_at DateTime @default(now()) @updatedAt

  @@map("documents")
}

model Subscription {
  id         Int       @id @default(autoincrement())
  name       String
  cost       Float
  category   String?   @default("General")
  next_date  DateTime?
  auto_renew Boolean?  @default(true)
  icon       String?   @default("📦")
  active     Boolean?  @default(true)
  created_at DateTime  @default(now())
  source     String    @default("manual")
  confidence Float     @default(1.0)
  updated_at DateTime  @default(now()) @updatedAt

  @@map("subscriptions")
}

model Habit {
  id              Int        @id @default(autoincrement())
  name            String
  icon            String?    @default("🏃")
  streak          Int?       @default(0)
  completed_dates String?    @default("[]")
  created_at      DateTime   @default(now())
  logs            HabitLog[]
  source          String     @default("manual")
  confidence      Float      @default(1.0)
  updated_at      DateTime   @default(now()) @updatedAt

  @@map("habits")
}

model Entertainment {
  id          Int      @id @default(autoincrement())
  title       String
  type        String?
  category    String?
  status      String?
  rating      Int?
  progress    Int?     @default(0)
  poster      String?
  created_at  DateTime @default(now())
  modified_at DateTime @default(now())
  source      String   @default("manual")
  confidence  Float    @default(1.0)
  updated_at  DateTime @default(now()) @updatedAt

  @@map("entertainment")
}

model HealthExtras {
  id                   Int      @id @default(autoincrement())
  vision_score         Int?
  hearing_score        Int?
  smell_score          Int?
  taste_score          Int?
  touch_score          Int?
  gut_biome_score      Int?
  dermatology_score    Int?
  hair_vitality_score  Int?
  bronco_level         Float?
  posture_status       String?
  active_diets         String?  @default("[]")
  hobbies              String?  @default("[]")
  hrv_score            Int?
  stress_load_score    Int?
  recovery_index_score Int?
  modified_at          DateTime @default(now())
  source               String   @default("manual")
  confidence           Float    @default(1.0)
  updated_at           DateTime @default(now()) @updatedAt

  @@map("health_extras")
}

model MoodLog {
  id         Int      @id @default(autoincrement())
  date       DateTime
  mood       Int?
  energy     Int?
  note       String?
  created_at DateTime @default(now())
  source     String   @default("manual")
  confidence Float    @default(1.0)
  updated_at DateTime @default(now()) @updatedAt

  @@map("mood_logs")
}

model HabitLog {
  id         Int      @id @default(autoincrement())
  habit_id   Int
  date       DateTime
  created_at DateTime @default(now())
  habit      Habit    @relation(fields: [habit_id], references: [id], onDelete: Cascade)
  source     String   @default("manual")
  confidence Float    @default(1.0)
  updated_at DateTime @default(now()) @updatedAt

  @@unique([habit_id, date])
  @@map("habit_logs")
}

model GoalProgressLog {
  id         Int      @id @default(autoincrement())
  goal_id    Int
  value      Float
  note       String?
  date       DateTime
  created_at DateTime @default(now())
  goal       Goal     @relation(fields: [goal_id], references: [id], onDelete: Cascade)
  source     String   @default("manual")
  confidence Float    @default(1.0)
  updated_at DateTime @default(now()) @updatedAt

  @@map("goal_progress_logs")
}

model NutritionLog {
  id         Int      @id @default(autoincrement())
  date       DateTime
  meal_type  String?
  food_name  String?
  calories   Float?
  protein    Float?
  carbs      Float?
  fat        Float?
  created_at DateTime @default(now())
  source     String   @default("manual")
  confidence Float    @default(1.0)
  updated_at DateTime @default(now()) @updatedAt

  @@map("nutrition_logs")
}

model WorkoutSession {
  id               Int               @id @default(autoincrement())
  date             DateTime
  notes            String?
  duration_minutes Int?
  created_at       DateTime          @default(now())
  exercises        WorkoutExercise[]
  source           String            @default("manual")
  confidence       Float             @default(1.0)
  updated_at       DateTime          @default(now()) @updatedAt

  @@map("workout_sessions")
}

model WorkoutExercise {
  id            Int            @id @default(autoincrement())
  session_id    Int
  exercise_name String
  sets          Int?
  reps          Int?
  weight_kg     Float?
  notes         String?
  created_at    DateTime       @default(now())
  session       WorkoutSession @relation(fields: [session_id], references: [id], onDelete: Cascade)
  source        String         @default("manual")
  confidence    Float          @default(1.0)
  updated_at    DateTime       @default(now()) @updatedAt

  @@map("workout_exercises")
}

model ProgressPhoto {
  id         Int      @id @default(autoincrement())
  date       DateTime
  url        String
  notes      String?
  created_at DateTime @default(now())
  source     String   @default("manual")
  confidence Float    @default(1.0)
  updated_at DateTime @default(now()) @updatedAt

  @@map("progress_photos")
}

model VitalsLog {
  id         Int      @id @default(autoincrement())
  date       DateTime
  type       String
  value      Float?
  unit       String?
  created_at DateTime @default(now())
  source     String   @default("manual")
  confidence Float    @default(1.0)
  updated_at DateTime @default(now()) @updatedAt

  @@map("vitals_logs")
}

model Medication {
  id         Int       @id @default(autoincrement())
  name       String
  dose       String?
  frequency  String?
  start_date DateTime?
  end_date   DateTime?
  created_at DateTime  @default(now())
  source     String    @default("manual")
  confidence Float     @default(1.0)
  updated_at DateTime  @default(now()) @updatedAt

  @@map("medications")
}

model SavedArticle {
  id         Int      @id @default(autoincrement())
  url        String
  title      String?
  source     String?
  date_saved DateTime @default(now())
  confidence Float    @default(1.0)
  updated_at DateTime @default(now()) @updatedAt

  @@map("saved_articles")
}

// --- Phase 1: Core Personal OS Entities ---

model Project {
  id          Int      @id @default(autoincrement())
  name        String
  description String?
  repository  String?
  commits     Int      @default(0)
  stars       Int      @default(0)
  source      String   @default("manual")
  confidence  Float    @default(1.0)
  created_at  DateTime @default(now())
  updated_at  DateTime @default(now()) @updatedAt

  @@map("projects")
}

model SkillEntity {
  id         Int      @id @default(autoincrement())
  name       String
  knowledge  Float    @default(0)
  retention  Float    @default(0)
  source     String   @default("manual")
  confidence Float    @default(1.0)
  created_at DateTime @default(now())
  updated_at DateTime @default(now()) @updatedAt

  @@map("skill_entities")
}

model LearningEvent {
  id         Int      @id @default(autoincrement())
  title      String
  type       String   @default("Course")
  hours      Float    @default(0)
  source     String   @default("manual")
  confidence Float    @default(1.0)
  created_at DateTime @default(now())
  updated_at DateTime @default(now()) @updatedAt

  @@map("learning_events")
}

model HealthMetric {
  id          Int      @id @default(autoincrement())
  date        DateTime @default(now())
  metric_type String
  value       Float
  unit        String?
  notes       String?
  source      String   @default("manual")
  confidence  Float    @default(1.0)
  created_at  DateTime @default(now())
  updated_at  DateTime @default(now()) @updatedAt

  @@map("health_metrics")
}
